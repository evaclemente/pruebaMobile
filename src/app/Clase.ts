export class Clase {

    id: string;
    admin: string;
    avatares: boolean;

    constructor(id: string, admin: string, avatares: boolean) {
      this.id = id;
      this.admin = admin;
      this.avatares = avatares;
    }
}
